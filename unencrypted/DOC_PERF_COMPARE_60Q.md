# 加密机制对RAG性能影响：Multi/Single/Null（60题）对比实验报告

## 1. 实验说明
- 数据集：Multi/Single/Null 各 60 题（lihua-queries1/2/3）
- 加密侧：encrypted_documents_lihua（加密payload）
- 明文侧：plaintext_documents_lihua_world（明文payload，独立Qdrant storage）

## 2. 分测试集延迟统计（median / p95）
| dataset | metric | encrypted median(s) | encrypted p95(s) | plaintext median(s) | plaintext p95(s) | ratio(median) |
|---|---|---:|---:|---:|---:|---:|
| Multi | retrieval_time_s | 0.1668 | 0.4810 | 0.1644 | 0.2389 | 1.015x |
| Multi | generation_time_s | 67.6807 | 83.2686 | 66.3946 | 77.6815 | 1.019x |
| Multi | total_time_s | 67.9246 | 83.4361 | 66.5591 | 78.0715 | 1.021x |
| Single | retrieval_time_s | 0.3370 | 0.5126 | 0.3609 | 0.4876 | 0.934x |
| Single | generation_time_s | 69.8988 | 84.0519 | 72.8209 | 84.7707 | 0.960x |
| Single | total_time_s | 70.0662 | 84.5366 | 73.2760 | 85.1043 | 0.956x |
| Null | retrieval_time_s | 0.1728 | 0.3216 | 0.1680 | 0.2494 | 1.029x |
| Null | generation_time_s | 68.1093 | 76.1266 | 67.3907 | 77.4540 | 1.011x |
| Null | total_time_s | 68.2677 | 76.3727 | 67.5805 | 77.6078 | 1.010x |

## 3. OK率（系统是否成功走RAG路径）
| dataset | encrypted ok_rate | plaintext ok_rate |
|---|---:|---:|
| Multi | 0.867 | 0.867 |
| Single | 1.000 | 1.000 |
| Null | 0.983 | 1.000 |

## 4. 论文可用结论要点（模板）
1. **总体端到端延迟**：以 `total_time_s` 的 median/p95 为主对比指标。
2. **检索阶段开销**：以 `retrieval_time_s` 对比，量化加密payload读取与解密带来的额外成本。
3. **生成阶段差异**：以 `generation_time_s` 对比。若两者接近，说明加密主要影响检索与上下文构建，而非LLM生成。
4. **分类别讨论**：对 Multi/Single/Null 分别给出加密开销倍数，说明问题类型对检索路径的影响。

## 5. 最终结论（可直接用于论文）

### 5.1 响应速度（基于完整 60Q 统计：run_id=20260414_125832）

端到端响应时间 `total_time_s` 的中位数（median）对比结果如下（加密/明文，括号内为加密相对明文的倍率）：

- **Multi**：67.9246s vs 66.5591s（**1.021x**）
- **Single**：70.0662s vs 73.2760s（**0.956x**）
- **Null**：68.2677s vs 67.5805s（**1.010x**）

检索阶段 `retrieval_time_s` 的 median 对比：

- **Multi**：0.1668s vs 0.1644s（**1.015x**）
- **Single**：0.3370s vs 0.3609s（**0.934x**）
- **Null**：0.1728s vs 0.1680s（**1.029x**）

**解释**：在本实验环境中，LLM 生成耗时（约 60–80s）远大于检索耗时（约 0.16–0.36s），因此加密机制带来的解密与 payload 处理开销在端到端时延中占比很小，整体表现为低百分比级别的波动（约 -4.4% 到 +2.1% 的 median 波动范围）。

### 5.2 资源占用与存储开销（基于资源测量补跑：run_id=20260414_123957）

为量化加密带来的**存储膨胀**与**进程资源占用差异**，对两套系统在 동일环境下进行补充测量（保留相同 embedding/检索参数与同一进程内测量 RSS/CPU）：

- **Qdrant 目录大小**（storage footprint）：
  - 加密：10.7 MiB
  - 明文：10.1 MiB
  - **差异**：+0.6 MiB（约 **+5.9%**）

- **进程 RSS 峰值**（内存占用峰值）：
  - 加密：677.2 MiB
  - 明文：676.9 MiB
  - **差异**：+0.3 MiB（可忽略）

- **CPU percent 峰值**（粗粒度观测）：
  - 加密：9.1
  - 明文：4.4
  - **差异**：加密侧峰值更高，但该指标受采样窗口影响较大（psutil 采样属于近似观测），建议在论文中将其作为“趋势性证据”，并用端到端延迟作为主结论依据。

### 5.3 综合结论与工程启示

1. **性能影响总体较小**：在本项目设置下，加密机制对端到端响应速度的影响较弱；主要原因是生成阶段占主导。
2. **开销主要体现在存储与检索路径**：加密 payload 会带来一定存储膨胀（本实验约 5.9%），并可能导致检索阶段的尾部延迟（p95）上升。
3. **安全收益与成本权衡**：在以隐私/合规为约束的应用场景中，该加密机制以较低的性能代价换取了“向量库落盘不包含原文”的安全收益；在生成耗时占主导的本地 LLM 场景下，该权衡更具可接受性。

---
本报告由 `summarize_perf_results.py` 自动生成。

## 摘要结论（可直接粘入论文）

在对 Multi/Single/Null 三类各 60 题的对比实验（run_id=20260414_125832）中，端到端中位延迟显示加密方案对总体性能影响极小：Multi 为 67.92s（加密） vs 66.56s（明文，1.021×），Single 为 70.07s vs 73.28s（0.956×），Null 为 68.27s vs 67.58s（1.010×）。检索阶段中位延迟仅约 0.16–0.36s，远小于 LLM 生成耗时（约 60–80s），因此加密引入的解密与 payload 处理在端到端延迟中占比有限；存储方面加密导致约 +5.9% 的目录膨胀。综上，对于以本地 LLM 生成为主的部署场景，将 payload 加密以换取“磁盘不含明文”的隐私收益，其性能成本较低且工程上可接受；推荐后续关注检索尾延迟与存储优化以进一步降低开销。
