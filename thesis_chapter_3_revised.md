# 第3章 系统设计

## 3.1 系统需求分析

### 3.1.1 功能需求

基于系统设计目标，本节明确系统的六项核心功能需求。

**需求F1：文档导入与管理**。系统需支持多格式文档解析，包括TXT、PDF、DOCX和Markdown等常见格式，能够完成文档解析、文本分块处理和加密存储，并生成导入统计报告。

**需求F2：加密存储**。用户导入的文档内容通过AES-256-GCM加密后存储在向量数据库中，加密密钥由用户本地保管。加密过程使用随机Nonce，确保同一文本的多次加密产生不同的密文，防止模式分析攻击。

**需求F3：语义检索**。系统支持基于向量相似度的自然语言查询，支持中英文混合查询，能够返回Top-K个最相关的文档片段，每个结果包含解密后的文本内容、来源信息和相似度分数。

**需求F4：隐私保护问答**。系统在本地完成检索、解密、上下文构建和答案生成的完整流程。用户数据不发送至外部服务器，答案应有明确的文档依据。

**需求F5：智能拒答**。当检索结果为空或LLM生成答案质量低下时（如明确表达"不知道"或答案过短），系统主动拒答而非生成推测性答案。

**需求F6：可视化界面与命令行**。系统提供两种交互方式：命令行接口（CLI）用于脚本集成和批量处理，Web UI用于交互式问答和文档管理。

### 3.1.2 非功能需求

**需求NF1：轻量化**。系统能在16GB内存的消费级设备上运行，不依赖独立显卡。Embedding模型（all-MiniLM-L6-v2）约80MB，量化后的LLM（Mistral-7B Q4）约4GB，两者合计远低于系统内存限制。

**需求NF2：性能**。向量检索在毫秒级完成（基于HNSW索引），LLM生成速度取决于模型推理能力。系统整体端到端响应时间应在用户可接受范围内（通常秒级到分钟级）。

**需求NF3：安全性**。文档内容通过AES-256-GCM加密保护，加密数据能抵御密码分析攻击。系统记录审计日志，通过链式哈希机制确保日志不可篡改。

**需求NF4：可维护性**。系统代码采用分层模块化设计，各模块接口定义清晰，便于独立测试和功能扩展。

### 3.1.3 运行环境约束

**硬件环境**：目标运行环境为消费级个人计算机，需具备16GB以上内存，不依赖独立显卡。系统设计允许在8GB内存设备上运行，但可能出现交换区使用，性能会有明显下降。

**软件环境**：操作系统支持Windows或Linux发行版，Python版本不低于3.8。系统不依赖CUDA，NVIDIA GPU非必需但如果可用会提升LLM推理速度。

---

## 3.2 系统总体架构设计

### 3.2.1 分层架构设计

为降低系统耦合度并提高可扩展性，本系统采用分层架构设计，将整个系统划分为四个层次。

**数据层**：负责系统数据的持久化存储，包括向量数据库（Qdrant本地模式）、加密密钥存储和审计日志三个核心组件。数据层为上层提供统一的数据访问接口，屏蔽底层存储实现的细节。

**服务层**：封装系统的核心业务逻辑，包括：
- 文档处理服务：文档解析和递归字符分块
- 加密服务：AES-256-GCM的加密和解密操作
- 检索服务：向量检索和可选的结果重排序
- 生成服务：与本地大语言模型的交互

各服务通过模块化设计实现职责分离，便于独立测试与替换。

**应用层**：实现了端到端的RAG流程，包括知识库构建流程（文档从导入到加密存储）和在线问答流程（用户查询从检索到答案生成）。应用层将服务层的原子能力组合为完整的业务场景，实现流程编排与状态管理。

**接口层**：提供了系统与用户的交互接口，包括命令行接口（CLI）和可视化界面（Streamlit Web UI）。接口层负责用户输入解析、结果展示和异常处理，对用户屏蔽系统内部实现细节。

```
┌──────────────────────────────────────────────────────────────────┐
│                   接口层（Interface Layer）                        │
│  ┌──────────────┐                      ┌──────────────┐          │
│  │  命令行CLI    │                      │ Web UI       │          │
│  │             │                      │(Streamlit)  │          │
│  └──────────────┘                      └──────────────┘          │
└──────────────────────────────────────────────────────────────────┘
               ↓                              ↓
┌──────────────────────────────────────────────────────────────────┐
│                  应用层（Application Layer）                       │
│  ┌─────────────────┐          ┌────────────────────┐             │
│  │知识库构建流程    │          │   在线问答流程      │             │
│  │（离线处理）      │          │  （实时问答）       │             │
│  └─────────────────┘          └────────────────────┘             │
└──────────────────────────────────────────────────────────────────┘
               ↓                              ↓
┌──────────────────────────────────────────────────────────────────┐
│                  服务层（Service Layer）                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │文档处理  │  │加密服务  │  │检索服务  │  │生成服务  │         │
│  │          │  │(AES-GCM) │  │(向量检索)│  │(LLM调用) │         │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘         │
└──────────────────────────────────────────────────────────────────┘
               ↓                              ↓
┌──────────────────────────────────────────────────────────────────┐
│                  数据层（Data Layer）                              │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐    │
│  │ 向量数据库       │  │ 加密密钥存储     │  │ 审计日志     │    │
│  │(Qdrant本地模式)  │  │(本地密钥文件)    │  │(链式哈希)   │    │
│  └──────────────────┘  └──────────────────┘  └──────────────┘    │
└──────────────────────────────────────────────────────────────────┘
```

### 3.2.2 核心数据流设计

系统包含两条主要数据流：离线的知识库构建流和在线的问答流。两条数据流共享相同的Embedding模型和加密密钥。

**知识库构建数据流**（离线）：

```
原始文档 → 解析提取 → 文本分块 → Embedding向量化 → 加密处理 → Qdrant入库
```

流程说明：原始文档通过文档解析器提取文本内容。经文本分块器切分为适当大小的块（512字符，重叠50字符）。每个块经Embedding模型（all-MiniLM-L6-v2）转换为384维向量表示。同时通过加密模块使用AES-256-GCM算法加密文本内容。最后将向量（明文）和加密Payload一起存入Qdrant向量数据库。

**在线问答数据流**（实时）：

```
用户查询 → Embedding向量化 → 向量检索 → 解密结果 → 上下文构建 
→ Prompt组装 → LLM生成 → 答案质量检查 → 答案返回
```

流程说明：用户查询通过Embedding模型转换为查询向量。在Qdrant中进行Top-K检索，获取候选结果。检索返回的加密Payload通过解密还原为明文。若检索结果为空，触发第一阶段拒答（无上下文硬拒答）。若上下文非空，通过ContextBuilder将检索结果和查询组织成完整的Prompt。将Prompt发送给本地LLM生成答案。生成后通过启发式规则（检测"I don't know"、答案长度等）检查答案质量。若答案低质量，触发第二阶段拒答。否则返回答案。

### 3.2.3 模块依赖关系

系统的各模块之间存在清晰的单向依赖关系：

```
┌─────────────────────────────────────────────────────────────────┐
│                      模块依赖关系图                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                    ┌──────────────┐                              │
│                    │ CLI / Web UI │                              │
│                    └──────┬───────┘                              │
│                           │                                      │
│                    ┌──────▼───────┐                              │
│                    │RAG主流程     │                              │
│                    │(rag_system)  │                              │
│                    └──────┬───────┘                              │
│                           │                                      │
│        ┌──────────────────┼──────────────────┐                   │
│        │                  │                  │                   │
│   ┌────▼────┐        ┌────▼────┐      ┌────▼────┐              │
│   │文档处理  │        │检索器    │      │LLM客户端 │              │
│   │模块      │        │(Retriever)│     │(Ollama) │              │
│   └────┬────┘        └────┬────┘      └─────────┘              │
│        │                  │                                      │
│        │         ┌────────┼────────┐                             │
│        │         │        │        │                             │
│        ▼         │        ▼        ▼                             │
│    ┌────────┐    │   ┌────────┐                                 │
│    │Embedding│    │   │向量存储 │                                 │
│    │模型     │    │   │(Qdrant)│                                 │
│    └────┬───┘    │   └────┬───┘                                 │
│         │        │        │                                      │
│         │        └────────┼────────┐                             │
│         │                 │        │                             │
│         │         ┌───────▼──────┐ │                             │
│         └────────►│加密模块      │◄┘                             │
│                  │(AES-256-GCM) │                              │
│                  └───────┬──────┘                              │
│                          │                                      │
│                  ┌───────▼──────┐                              │
│                  │密钥管理      │                              │
│                  │(本地文件)    │                              │
│                  └──────────────┘                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

依赖关系遵循单向原则：上层模块依赖下层模块，下层模块不依赖上层模块，从而确保系统的可测试性和可维护性。加密模块作为核心安全组件，被文档处理模块和检索模块共同依赖。密钥管理模块为加密模块提供密钥服务，自身不依赖其他业务模块。

---

## 3.3 隐私保护机制设计

第2章定义了系统面临的四类威胁（T1–T4）。本节围绕这四类威胁，从系统设计层面阐述相应的防御机制。

| 威胁编号 | 威胁描述 | 对应安全机制 |
|---------|---------|------------|
| T1 | 存储介质泄露 | 混合加密策略（明文向量+加密Payload） |
| T2 | 未授权访问 | 密钥管理与访问控制 |
| T3 | 传输窃听 | 本地部署架构（无跨网络传输） |
| T4 | 向量反演攻击 | 混合加密策略（安全边界） |

### 3.3.1 混合加密策略

传统加密方案存在一个两难困境：完全加密向量会破坏语义信息，导致无法进行检索；不加密则无法保护数据隐私。本研究提出"混合加密策略"，在保证检索效率的同时提供数据隐私保护。

**核心设计思想**：对文档内容进行加密以保护隐私，但保留用于检索的向量表示。系统将文档数据分为两部分存储：
- **向量数据**：保持明文存储，用于HNSW索引和高效相似度计算
- **Payload数据**：包含原始文档片段、文档来源等敏感信息，采用AES-256-GCM加密后存储

这种设计的安全性来源于：当攻击者获得向量数据库的访问权限时，只能获取向量数据和加密后的Payload数据。由于无法获得解密密钥，攻击者无法还原原始文档内容。

**向量反演攻击的安全边界**（威胁T4）：向量本身包含一定的语义信息，在理论上存在被反演还原的风险。但反演需要专门的攻击模型和计算资源，且还原精度有限。本研究在混合加密策略中通过以下设计降低此风险：
- Payload加密保护原始文本，即使向量被部分反演，攻击者也无法获得完整原始文档
- 这是本系统在性能与安全之间的显式权衡决策
- 对于T4防御要求更高的场景，可在未来版本引入向量扰动或差分隐私技术

**加密数据格式**：每个文本块在Qdrant的Payload中包含：
- `ciphertext`：AES-GCM加密后的密文（Base64编码）
- `nonce`：AES-GCM使用的随机数（Base64编码，12字节）
- `source_file`：文档来源（可选进行脱敏处理）
- `chunk_id`：块编号用于追踪

在正常隐私保护模式下，系统不存储任何明文字段，确保隐私保护的完整性。

### 3.3.2 密钥管理与访问控制

密钥管理是加密系统安全性的关键环节。本系统设计了完整的密钥生命周期管理方案。

**密钥生成**：系统支持两种密钥获取方式。方式一是用户指定口令，系统通过PBKDF2算法从口令派生256位密钥（盐值随机生成，迭代次数100000）。方式二是用户直接提供密钥文件。密钥长度固定为256位，与AES-256-GCM的密钥要求一致。

**密钥存储**：密钥文件存储在用户本地文件系统（如`encryption.key`），用户需妥善保管。系统不在任何远程服务器上保留副本。一旦丢失，已加密存储的数据将无法解密。

**密钥使用**：每次加密操作使用新生成的随机数（Nonce，12字节），确保相同明文每次加密产生不同的密文，防止统计分析攻击。

**销毁机制**：用户删除密钥文件后，存储在向量数据库中的密文将无法解密，从而实现数据的逻辑删除。

**访问控制**（威胁T2）：系统以密钥作为访问控制的凭证。只有能够提供正确解密密钥的调用者才能读取文档内容。这一设计利用密码学手段而非传统的账号口令体系，与本地部署的整体架构保持一致。

系统遵循"最小明文暴露原则"：明文数据在内存中按需生成，不进行持久化存储，处理完成后相关内存引用可被垃圾回收机制自动释放。检索阶段仅操作密文Payload，只在返回用户前进行最后的解密和输出。

### 3.3.3 审计日志机制

审计日志是系统安全性的重要保障，用于记录系统运行过程中的关键操作事件，便于事后追溯和安全审计。

**审计日志的设计遵循三项原则**：

1. **完整性保护**：采用链式哈希机制确保日志不可篡改。每条日志记录包含当前操作的SHA256哈希值以及前一条日志的哈希值，形成链式结构。系统提供`verify_integrity()`方法供手动验证日志完整性。若任何历史日志被修改，链会被破坏，通过完整性校验可检测篡改行为。

2. **隐私保护**：审计日志仅记录操作事件的元信息（操作类型、时间戳、涉及文档数量等），不记录完整的查询内容、回答内容或文档片段。查询内容在日志中截断处理（仅保留前200字符），既保留基本的查询特征用于审计追溯，又避免完整敏感信息泄露。

3. **可追溯性**：日志记录覆盖系统的主要操作类型，包括系统初始化、文档导入、查询请求、LLM调用、检索操作、错误记录等。

**存储隔离设计**：审计日志文件与向量数据库文件、密钥文件在存储位置上相互隔离，分别存储在不同的目录路径。该设计实现了安全域的分离——即使向量数据库文件或密钥文件被意外泄露，审计日志的完整性和可追溯性仍能得到保障，从而支持事后取证分析。

**性能影响**：审计日志采用追加写模式，单次写入数据量极小（通常不超过256字节）。在本地文件系统上追加写的延迟通常为微秒级，该机制对系统整体性能的影响可忽略不计。

---

## 3.4 知识库构建流程设计

### 3.4.1 文档解析与预处理设计

文档解析是将各类格式的原始文档转换为统一文本格式的过程。本系统支持TXT、PDF、DOCX和Markdown四种常见文档格式的解析。

系统通过格式识别模块自动判断文档类型，并调用相应的解析器进行文本提取。具体包括：
- **TXT格式**：直接读取文件内容并处理编码问题
- **PDF格式**：通过pypdf等库提取文本内容（仅支持纯文本PDF，不支持扫描版）
- **DOCX格式**：解析文档结构并提取纯文本内容
- **Markdown格式**：作为纯文本读取并保留段落结构

预处理阶段对提取的原始文本进行清洗，包括移除多余的空白字符、统一换行符格式、处理特殊字符和不可见字符、移除页眉页脚等干扰信息。

### 3.4.2 递归字符分块策略设计

文本分块是将长文档切分为适合检索和上下文长度的小片段。分块质量直接影响检索效果和生成质量：分块过大导致检索精度下降，分块过小导致语义不完整。

**分块策略选择理由**：本研究选择递归字符分块策略，主要基于以下考虑：
1. **语义保持能力**：递归分块能够较好地保持语义完整性，优先在句子、段落边界处切割，避免截断关键句子
2. **实现效率高**：相比基于语义的分块策略，递归字符分块不需要额外的Embedding模型推理，计算开销小
3. **适应性好**：能够适应不同格式的文档，包括TXT、PDF、DOCX等

**分块参数配置**：
- **块大小（chunk_size）**：512个字符
- **重叠大小（chunk_overlap）**：50个字符

参数选取的理由：主流大语言模型的上下文窗口通常支持2048 token以上。512字符约等于256–384 token，在单次检索中能够返回多个相关块，同时避免单个块过大导致检索粒度粗糙。该参数设置将在第5章中通过实验验证其有效性。

### 3.4.3 向量化与加密入库流程设计

**Embedding模型选择**：在本地资源受限场景下，需在模型性能与计算开销之间进行权衡。本研究选择轻量级Embedding模型all-MiniLM-L6-v2，主要基于以下考虑：
1. **轻量化**：该模型仅有约22MB大小，适合消费级硬件环境
2. **速度快**：推理速度快，能够满足实时检索的需求
3. **多语言支持**：该模型支持多语言Embedding，包括中英文场景
4. **维度合适**：384维的向量表示在内存占用和表达能力之间取得平衡

**加密处理**：使用AES-256-GCM加密算法对文本块内容进行加密。每个块的加密过程使用新的随机Nonce，确保加密的安全性和不可预测性。

**Qdrant入库**：将向量、加密Payload和元数据组装为Qdrant的Point结构，使用批量upsert操作将数据写入向量数据库。批量写入相比逐条插入能够显著提升导入效率。

---

## 3.5 在线问答流程设计

### 3.5.1 检索设计

**向量检索阶段**：用户输入的自然语言查询经过Embedding模型转换为查询向量，在Qdrant中进行Top-K相似度检索。Qdrant使用HNSW（Hierarchical Navigable Small World）索引进行近似最近邻搜索，检索速度对数据库规模不敏感，通常在毫秒级完成。

系统在实现中采用了查询优化策略：对于时间相关和邮件查询，系统实现了基于电子邮件头部特征的优化。若向量检索结果中不包含邮件头部信息，则进行有限的本地扫描（上限约1000条记录）以发现包含时间戳的块。这种针对性的后备方案能够提升特定查询类型的检索准确率。

**可选的重排序阶段**：系统支持可选的结果重排序功能，对向量检索的候选结果进行精细化打分。重排序通过配置文件灵活开启或关闭，用户可根据硬件条件和性能需求灵活选择。

重排序实现采用双模式设计：
- **高精度模式**：使用CrossEncoder模型（`cross-encoder/ms-marco-MiniLM-L-6-v2`）对候选结果进行语义相关性打分，精度较高但需额外计算资源
- **轻量级模式**：使用基于词项重叠度的启发式规则（LocalReranker），包括Jaccard相似度、精确匹配奖励等，无需额外模型，适合资源受限环境

两种模式均不依赖外部API，满足隐私保护要求。

### 3.5.2 上下文构建与Prompt设计

**上下文格式**：将检索结果按相关性降序排列，格式化为编号列表，每个结果包含文档来源和文本内容。

**Prompt设计原则**：
1. **明确答案来源**：要求模型在回答时引用文档来源（如[1]、[2]），便于用户验证
2. **限制回答范围**：明确告知模型仅根据提供的文档内容回答，避免幻觉答案
3. **拒答引导**：在Prompt中明确指示模型，当上下文信息不足以回答问题时，应直接回复"I don't know"，避免生成推测性答案

### 3.5.3 智能拒答机制设计

智能拒答机制是系统在检索结果不足以回答用户问题时，主动进行拒答的功能。本系统的拒答机制基于**两阶段决策逻辑**。

**第一阶段：无上下文硬拒答**。若检索与重排后无法构建有效上下文（context为空或所有候选块均无法解密），系统直接返回拒答，避免大语言模型基于错误信息生成答案。此阶段**不调用LLM**，降低无效计算。

**第二阶段：LLM输出质量评估**。若上下文非空，系统调用LLM生成答案。生成后，系统通过启发式规则检测答案质量。规则包括：
- 答案为空或过短（少于20字符）
- 答案包含预定义的拒答模式（"I don't know"、"Insufficient information"等）
- 若检测到上述情况，判定为低质量答案并触发拒答

**置信度计算**（用于第5章实验分析）：系统计算置信度分数用于量化评估答案质量：

$$C = 0.6 \times S_{\text{rerank}} + 0.2 \times L + 0.2 \times Q$$

其中：
- $S_{\text{rerank}}$：重排序得分（0-1范围）
- $L$：答案长度因子（答案字符数>20则为1，否则为0）
- $Q$：输出质量因子（有效答案为1，低质量为0）

最终置信度经归一化处理后取值为[0, 1]区间。

**重要说明**：置信度公式主要用于第5章的实验评估和分析，系统的实际拒答决策**基于启发式规则而非置信度阈值**。这样的设计选择是为了避免过度依赖参数化阈值导致系统脆弱性，在用户体验（尽量回答）和系统安全性（避免幻觉）之间取得平衡。

---

## 3.6 本章小结

本章围绕"隐私保护为主线、轻量化为支撑"的核心设计目标，完成了面向隐私保护的轻量级RAG系统的完整方案设计。

**设计成果包括**：
1. 明确的需求分析和运行环境约束
2. 分层架构设计明确了各层职责和接口
3. 混合加密策略在性能和安全性之间取得平衡
4. 完整的密钥管理和审计日志机制
5. 两阶段拒答机制提升系统稳定性
6. 针对性的检索优化和上下文构建设计

上述设计方案为第4章的系统实现提供了结构基础，核心设计决策将在第5章中通过实验进行验证。
